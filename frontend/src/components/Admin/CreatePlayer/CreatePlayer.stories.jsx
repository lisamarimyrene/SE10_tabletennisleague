import CreatePlayer from "./CreatePlayer"

export default {
  component: CreatePlayer,
  title: 'Create player',
  tags: ['autodocs', 'create player'],
};

export const Default = {
  args: {
   
  },
};
